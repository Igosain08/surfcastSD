# Week 1 — Surfline / surfpy scoping (1-page deliverable)

**Owner:** _name_  
**Due:** end of Week 1  

## Instructions

Answer the questions below in **one page** (PDF or Markdown in `docs/`). **No production scraping code** in Week 1 unless the mentor approves.

## Questions to answer

1. **Library / access** — What is `surfpy` (or the path you use)? Link to repo or docs. Is it installable via `pip`? Last commit date?

2. **What can you actually retrieve?** — Example fields returned for one break (paste redacted sample JSON or dict keys). Rating: raw string or numeric? Timestamp granularity?

3. **Label mapping** — How do Surfline categories map to the project’s 1–5 scale? (Poor / Fair / Good / Epic / etc.) Document assumptions.

4. **Historical depth** — Roughly how far back can you get labels? Enough for ~1 year of training data?

5. **Risk** — ToS / scraping / rate limits. What breaks if Surfline changes HTML or blocks IPs? Mitigation (caching, backoff).

6. **Recommendation** — Feasible as Week 2 training labels **yes/no** + what you need from the team (API keys, test account, etc.).

## Notes

_Team notes / findings_
